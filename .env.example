# Environment Variables for RackSmith

# Release Mode - Controls authentication bypass
# dev = Bypass authentication, use mock data (for development)
# prod = Enforce authentication, use real API (for production)
RELEASE=dev

# API Base URL
API_URL=http://localhost:3000/api
